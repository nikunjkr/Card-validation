{
  "name": "cardvalidator",
  "version": "1.0.0",
  "description": "",
  "main": "valid.js",
  "scripts": {
    "dev": "node card_validator",
    "test": "node valid",
    "coverage": "npm run pretest && jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.1.0"
  }
}
